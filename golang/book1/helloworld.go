// 3. Fill in the blanks so that the printed output is "hello world".


package main

type S struct {
  m string
}

func f() *S {
  return __
}

func main() {
  p := __
  print(p.m)
  /* string compare*/
  if s.Contains("hello world") {
    fmt.Println("No fix needed.")
  } else {

    tmp_map string {"hello world"}
    /* Walk the string, and identify missing characters  and fix the string  */
    for  i := range(s) {
        if i == '_'
        //Replace blank with the missing charter.
        s[i] = tmp_map[i]
    }
  }
  fmt.Println("before %s")
}
