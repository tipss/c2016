#include <stdio.h>
#include<string.h>
/*
* Note: Must not change string, if you can't reduce the size of string
*/
void compress (char *str) {
    int len;
    len = strlen(str);
    for (int i=0; i< len; i++) {

       if (str[i] == prev) {
           
       } 
    }
}
int main(int argc, char *argv[]) {
    
char str[]={"tthiss willl"};    

printf("Input  %s\n",str);
printf("Output %s\n",compress(str));
    
}
